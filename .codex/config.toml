# Generated by /cenci:configure — discovers cenci's project agent adapters in .codex/agents/.
# Each entry loads its role config (model, reasoning effort, developer instructions) from the
# matching .toml file so native Codex runs use the same role tiering as the Claude Code agents.

[agents.planner]
config_file = "agents/planner.toml"

[agents.implementer]
config_file = "agents/implementer.toml"

[agents.critical-reviewer]
config_file = "agents/critical-reviewer.toml"

[agents.context-gatherer]
config_file = "agents/context-gatherer.toml"

[agents.structure-analyzer]
config_file = "agents/structure-analyzer.toml"
